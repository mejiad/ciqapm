package com.evoltech.ciqapm.repository;

import com.evoltech.ciqapm.model.documentos.DocumentoEntregable;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DocumentoEntregableRepository extends JpaRepository<DocumentoEntregable, Long> {
}